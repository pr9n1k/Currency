import React from 'react';
import Nav from './nav';

const Header = () => {
    return (
        <header className="bg-green px-10 py-4">
            <Nav />
        </header>
    );
};

export default Header;
